# Gamifying-QA
---------------------------------------------------------------------------

## Dependencies
Install `gym, numpy, pytorch, tqdm, pyyaml` on a `python 3.6` machine

## Play (human only for now)
`python play.py`